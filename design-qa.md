# 职学导航 V2 / V3 最终设计验收记录

## 1. 验收范围

- 页面：首页、资料审查、能力诊断、个性化资料库。
- 业务约束：保留现有 Vue 路由、登录保护、岗位接口、资料输入、诊断结果、资源列表和详情链路。
- 视觉方向：White + `#DEFADE` + 克制渐变绿 + Liquid Glass + Spatial AI。
- 数据原则：正式环境只读取真实接口；完整演示数据仅在 `VITE_PUBLIC_PREVIEW=true` 且 URL 带 `?demo=1` 时启用。

## 2. 参考图

| 页面 | 参考图 | 参考尺寸 |
| --- | --- | --- |
| 首页 | `D:/exe/ChatGPT Image 2026年8月16日 18_57_07 (1).png` | 1920 × 1080 |
| 资料审查 | `D:/exe/ChatGPT Image 2026年8月16日 18_57_08 (2).png` | 1920 × 1080 |
| 能力诊断 | `D:/exe/ChatGPT Image 2026年8月16日 18_57_08 (3).png` | 1920 × 1080 |
| 资料库 | `D:/exe/ChatGPT Image 2026年8月16日 18_57_08 (4).png` | 1920 × 1080 |

## 3. 同屏对照

- 首页 V3：`design-qa-assets/final-home-v3-reference-comparison.png`
- 资料审查：`design-qa-assets/compare-review-side-by-side.png`
- 能力诊断 Diagnostic Console V4：`design-qa-assets/final-diagnosis-console-v4-reference-comparison.png`
- 资料库 Hero：`design-qa-assets/final-library-cube-v3-reference-comparison.png`

每张对照图左侧为参考图、右侧为实际运行页面；首页与能力诊断采用 1680 × 945 主验收视口，资料审查与资料库采用 1920 × 1080 主验收视口，并等比例缩放到同一画布。

## 4. 视觉检查

### 4.1 全局设计系统

- 通过：主氛围色为 `#DEFADE`，白色为主背景，深绿只承担焦点与状态表达。
- 通过：Primary CTA、进度、核心数字和空间光源使用统一 Gradient Tokens，未出现大面积高饱和绿色。
- 通过：重要玻璃容器使用透明层、边缘折射、内高光与分层阴影，不是单一模糊效果。
- 通过：图标来自 Element Plus 图标库；首页、诊断与资料库空间对象使用独立位图资产。
- 通过：1920 宽屏内容区调整为 1680 px，与参考图的主体占比接近；1366 及以下仍使用流式宽度。

### 4.2 首页

- 通过：V3 Hero 主体高度收敛至 408 px，桌面端保持约 40/60 的文案与 AI Spatial Hub 比例，1680 × 945 下 Hero、流程、岗位、能力带与 Footer 构成一张紧凑产品画布。
- 通过：核心能力分、Radar、Agent 状态和学习路径采用不同宽高比与不同方向的轻微三维倾斜；前后轨道、双层核心和粒子共同形成空间遮挡关系。
- 通过：1366、1440、1680、1920 四档桌面宽度均无水平溢出，标题稳定保持两行，右下学习路径卡始终位于 Hub 安全边界内。

### 4.3 资料审查

- 通过：AI Workspace 是页面主工作区，右侧仅保留一个 Review Inspector。
- 通过：空状态包含 Agent 引导、材料入口、经历描述输入和协同流程，不是无信息白板。
- 通过：岗位选择会同步刷新岗位职责和技能标签；真实上传、对话和 Agent 执行逻辑保持原接口。

### 4.4 能力诊断

- 通过：真实空状态不展示伪造分数，明确引导用户先完成资料审查。
- 通过：完整数据态展示综合分、Radar、五维得分、证据置信度、真实结果准确率、Agent 结论和学习路径。
- 通过：中央 Diagnostic Core 采用外层透明轮廓、内层半透明面板、边缘折射、指针反光和底部接触光六层结构；平台素材透明度收敛至 24%，绿色仅保留在进度、图形轮廓与底部环境折射，不再形成大面积雾团。
- 通过：综合得分改为 ECharts 三层 Gauge，区分外玻璃环、进度环与内层刻度；能力雷达改为五层网格、双层 Polygon、中心高亮与独立轮廓光，Score 为强焦点、Radar 为主要分析图。
- 通过：中央面板内部按 `Score / Divider / Radar` 形成明确主控台结构，底部评估时间、证据数、诊断状态和 DEV 标记统一进入轻量状态栏；左右侧栏及下方结果区使用更弱的玻璃材质。
- 通过：1366、1440、1680、1920 四档桌面宽度下，左右侧栏与中央 Core 的安全间距分别为 17.1、18、21、22 px，中央 Glass 宽度分别为 660.9、733、780、780 px，无重叠、裁切或水平溢出。
- 通过：开发演示数据仅在公开预览构建的 `?demo=1` 中可用，正式构建始终读取真实 API。

### 4.5 资料库

- 通过：AI 推荐学习包为唯一主视觉，普通资源卡保持白色或半透明层级。
- 通过：推荐学习包采用 52/48 左文右图结构；右侧为独立 Spatial Stage，不再使用普通缩略图裁剪逻辑。
- 通过：立方体素材经完整外扩后以 `object-fit: contain`、中心锚点和径向边缘融合呈现，主体、底座与轨道完整可见。
- 通过：在 1366、1440、1680、1920 四档桌面视口中，视觉对象边界均完全位于 Stage 与 Hero 内；页面水平溢出均为 0。
- 通过：真实空状态仍展示搜索、来源门禁、资源类型预览、生成流程和明确 CTA。
- 通过：完整数据态支持关键词搜索、类型与知识点筛选、网格/列表视图、来源状态和资源详情。
- 通过：演示资源详情弹窗能显示 `source_chunk_id`、审核结论和来源核验状态。

## 5. 响应式截图

已检查以下视口：

- 1920 × 1080：四个页面，另含诊断与资料库真实空状态。
- 1440 × 900：四个页面，另含诊断与资料库真实空状态。
- 1680 × 945：首页 V3 主视觉与资料库 Hero 专项验收。
- 1366 × 768：四个核心完整状态。
- 390 × 844：首页、资料审查、能力诊断完整状态、资料库完整状态。

截图保存在 `design-qa-assets/`，命名格式为 `final-页面-视口.png`。

检查结果：导航、标题、按钮、筛选栏、数据图形、卡片和正文均未发生横向溢出、相互遮挡或不可读截断。

## 6. 交互与状态检查

| 检查项 | 结果 |
| --- | --- |
| 资料审查岗位下拉包含前端、后端、运维、产品四方向 | 通过 |
| 切换到产品经理后 Inspector 同步更新职责 | 通过 |
| 资料库搜索 `MySQL` 只保留对应资源 | 通过 |
| 点击演示资源显示来源 ID 与审核说明 | 通过 |
| 诊断页“查看推荐学习资料”进入对应资料库 | 通过 |
| 正式预览未登录访问 `/library` 跳转 `/login` | 通过 |
| 公开视觉预览仅解除路由拦截，不绕过真实 API 鉴权 | 通过 |
| 浏览器 warning/error 日志 | 0 条 |

## 7. 构建检查

- `vue-tsc -b`：通过。
- `vite build`：通过。
- 已知非阻断项：ECharts 构建产物超过 500 kB，属于体积优化提示，不影响页面功能与本次视觉验收。

## 8. 迭代记录

1. V1 基线：绿色主题已存在，但首屏缺少空间焦点，空状态信息不足，四页层级趋同。
2. V2 第一轮：统一 Token、玻璃材质、导航和三套空间资产，重构四页核心体验。
3. V2 第二轮：补齐诊断与资料库双状态、真实准确率展示、来源预览和完整学习资源卡。
4. V2 第三轮：使用参考图与运行截图同屏比较，将宽屏内容上限从 1450 px 调整为 1680 px，并复核全部断点。
5. V2 第四轮：依据四张参考图统一分层玻璃材质、柔和折射与低强度绿光；首页四张信息卡采用四组不同方向的轻微三维倾斜，并由 Hub 安全边界约束。
6. 定点布局复核：诊断页在 1366、1440、1680、1920 宽度下左右间距分别为 17、18、21、22 px，所有页面水平溢出均为 0；资料库左右侧栏底线与中间内容底线差值为 0 px。
7. 最终运行截图：`final-home-1920x1080.png`、`final-diagnosis-1920x1080.png`、`final-review-1920x1080.png`、`final-library-1920x1080.png`；浏览器 warning/error 为 0 条。
8. V3 首页构图收敛：Hero 压缩为 408 px，Hub 扩展为 60% 主视觉，四张 Spatial Card 建立不同尺寸、透视与前后关系；最终同屏对照为 `final-home-v3-reference-comparison.png`。
9. 资料库 Hero 专项修复：生成完整空间立方体素材并建立独立 `contain` 展示舞台。1366 × 768 下图像边界为 `(699, 319.9, 335 × 272)`、Hero 边界为 `(286, 290.9, 775 × 330)`；1680 × 945 下图像边界为 `(864.4, 328.2, 483.6 × 279)`、Hero 边界为 `(286, 299.2, 1089 × 337)`，两档均完整位于 Hero 内。
10. 能力诊断主控台 V4 定向重做：重新划分 Score 与 Radar 模块，以 ECharts 三层 Gauge 和双层 Holographic Radar 取代原单层图表；降低平台绿光与玻璃饱和度，保留透明边缘、悬浮接触光和受控环境折射。最终同屏对照为 `final-diagnosis-console-v4-reference-comparison.png`。

final result: passed
