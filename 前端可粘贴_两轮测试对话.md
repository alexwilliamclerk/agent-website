# 前端可粘贴的两轮测试对话

使用方式：每个编号是一名独立测试用户。先粘贴“第 1 轮”，等待资料审查 Agent 提出定向问题；再粘贴“第 2 轮”。两段都是**测试用户要输入前端的第一人称文字**，不是给开发者看的预期结果，也不是 Agent 提示词。

---

## 前端开发工程师

### FE-01 基础起步型

**第 1 轮**

我的目标职业是前端开发工程师。我已经学习 HTML、CSS 和 JavaScript 约半年，能够独立完成静态网页、表单校验和简单的页面布局。跟着课程做过待办清单和校园活动展示页，自己写过新增、删除、完成状态切换等交互，也会用 Git 提交代码。现在我能理解 DOM 操作、数组循环和事件绑定，但还没有独立使用 Vue 完成完整项目，也没有接触过真实后端接口、构建发布和多人协作流程。我希望系统根据我目前真实完成过的内容判断基础是否合格，并告诉我下一步最需要补哪几项能力。

**第 2 轮**

在待办清单项目中，我负责的是新增、完成和删除三个功能。用户输入内容后点击按钮，我把内容存到数组中并重新渲染列表；点击完成会给当前项加删除线，点击删除会从数组中移除。为了确认功能正常，我测试了空输入、连续添加多条任务、重复点击完成和删除最后一项这几种情况，页面没有报错。不过刷新浏览器后数据会丢失，我没有做本地存储，也没有接过接口，所以不清楚真实项目里的数据保存和请求失败应该怎么处理。

### FE-02 概念会但应用不足型

**第 1 轮**

我的目标职业是前端开发工程师。我学过 Vue 的组件、props、事件、路由和基础响应式，也看过 Pinia、生命周期和组合式 API 的课程。课程作业中我完成过一个电商商品列表和购物车页面，能把后端返回的商品名称、价格和图片展示到页面上，也会根据分类切换列表。我大多数代码是参照教程完成的，对接口异常、加载状态、分页、性能优化和团队协作没有系统实践。请根据我已经会的内容和不会的内容判断我处于什么水平，不要仅因为我提到 Vue 就认定我已经具备项目开发能力。

**第 2 轮**

商品列表页面由我负责接口联调。我用 axios 请求商品接口，传入分类和页码参数，再把返回的商品数组保存到页面状态中，使用 v-for 渲染卡片。请求发送后我会先显示“加载中”，如果接口返回失败则显示提示文字；我在浏览器 Network 面板里检查过请求参数、状态码和返回数据，也用空数组测试过页面不会崩溃。但我没有处理过登录失效、取消重复请求或复杂状态共享，购物车数据也只是页面内保存，离开页面后会消失。

### FE-03 项目实战进阶型

**第 1 轮**

我的目标职业是前端开发工程师。我独立完成过校园二手交易平台前端，使用 Vue3、TypeScript、Pinia 和 Vite，负责商品发布、搜索筛选、收藏、订单和个人中心页面。项目中我和后端同学约定接口字段，完成过登录、列表、详情和订单流程联调，并用 Git 分支提交和合并代码。我了解路由懒加载、图片压缩、组件拆分和基础权限控制，也做过接口错误提示。当前不足是没有做过完整的前端监控、自动化测试和复杂多人项目的状态治理，希望诊断时区分我真正做过的能力与只了解概念的能力。

**第 2 轮**

我处理过商品详情页首屏加载慢的问题。起初页面包含多张原图和推荐商品模块，打开详情页时图片请求很多。我用 Lighthouse 和浏览器 Network 面板查看资源体积与加载顺序，发现首屏图片过大、推荐模块也同时加载。后来我压缩图片并给非首屏图片增加懒加载，把推荐模块改为异步加载。修改后再次测试，首屏资源请求明显减少，Lighthouse 性能分数从 62 提升到 81。这个结果是我本地重复测试得到的，但我还没有接入线上性能监控。

---

## Java 后端开发工程师

### BE-01 Java 基础起步型

**第 1 轮**

我的目标职业是 Java 后端开发工程师。我学习 Java 基础、面向对象、集合和异常处理约半年，能写学生信息管理一类的控制台程序，会使用 ArrayList 保存数据，也尝试过简单文件读写。数据库目前会建表、插入、查询、更新和删除，能写基础 SQL，但没有真正做过 Web 项目。我还没有使用 Spring Boot，不清楚 REST 接口、参数校验、统一异常处理、登录认证和服务部署的实际做法。请根据我真实完成的练习判断当前能力层次，并生成适合我补强的学习路径，而不是直接按初级后端工程师标准给我打高分。

**第 2 轮**

学生信息管理程序中的查询功能由我自己写。我先遍历 ArrayList 中的学生对象，比较学号是否相同，找到后输出姓名、班级和成绩，没有找到时提示用户不存在该学生。为了验证，我测试了已存在学号、不存在学号和重复添加同一学号三种情况；重复数据目前只是手动判断后不加入列表。程序里我用 try-catch 处理了输入不是数字时的异常，但还没有连接 MySQL，也没有写接口或单元测试，数据关闭程序后就不保留了。

### BE-02 框架会用但工程不足型

**第 1 轮**

我的目标职业是 Java 后端开发工程师。我已经学过 Spring Boot、MyBatis 和 MySQL，跟着课程完成过图书管理系统，包含图书增删改查、分页查询和简单登录页面。我可以按照示例分 Controller、Service 和 Mapper 编写代码，也会用 Postman 测试接口，知道 Maven 的基本使用。现在我的问题是大多数功能都是按教程模仿完成的，对数据库索引、事务、接口异常规范、权限认证、Redis 和部署不够熟悉。我希望系统能识别我“会运行项目”和“能独立解释并完成需求”之间的差距。

**第 2 轮**

图书新增接口中，我负责校验 ISBN 和保存数据。Controller 接收前端传来的图书信息，Service 先查询 ISBN 是否已经存在，不存在才调用 Mapper 插入数据库；如果重复就返回提示。我使用 Postman 分别测试了正常新增、ISBN 重复和缺少书名的情况，接口能返回不同结果。代码里目前是自己写 if 判断和返回文字，没有做统一异常处理，也没有涉及事务。一次删除图书关联借阅记录的问题我不知道怎么处理，所以当时只是按照老师给的 SQL 继续修改。

### BE-03 项目实战进阶型

**第 1 轮**

我的目标职业是 Java 后端开发工程师。我参与开发过校园电商订单系统，使用 Spring Boot、MySQL、Redis、JWT、Docker 和 Nginx。我的主要工作包括商品和订单接口、登录鉴权、订单状态查询以及 Redis 缓存热点商品；我与前端约定过接口字段，也通过 Git 分支参与协作。项目部署在测试服务器上，我能查看容器日志和处理常见配置错误。我理解索引、事务和缓存一致性的基础概念，但没有做过大规模高并发、消息队列削峰和正式生产事故处理。请按证据判断我的实际工程能力和下一阶段需要补的内容。

**第 2 轮**

我处理过订单查询接口变慢的问题。通过慢查询日志发现订单表按用户和时间查询时没有走合适索引，接口在测试数据增加后明显变慢。我和同学确认查询条件后新增了 user_id、created_at 的联合索引，并用 EXPLAIN 对比执行计划；对于重复访问的订单汇总信息增加了 Redis 缓存，同时设置订单状态变更后删除对应缓存。修改后我用同一批测试数据重复请求接口，查询耗时下降明显。这个项目没有压测到很高并发，也没有使用消息队列。

---

## 运维工程师

### OPS-01 基础起步型

**第 1 轮**

我的目标职业是运维工程师。我学习 Linux 和服务器基础约一年，能够使用常见 Linux 命令，理解用户、权限、进程、端口和日志的基本概念。跟着课程部署过 Nginx、Docker Compose 和一个小型项目，配置过环境变量、端口映射和反向代理，也会查看容器状态和日志。但很多操作都是参考教程完成的，我还没有独立做过完整的部署文档、故障排查记录、监控告警、安全加固或备份恢复。请结合我实际说出的内容判断我处于基础、进阶还是项目实践阶段，并指出哪些能力需要补充证据。

**第 2 轮**

我部署 Nginx 容器时遇到过浏览器访问不了页面的问题。我先用 docker ps 确认容器在运行，再用 docker logs 查看日志，没有看到启动报错；之后检查启动命令发现宿主机端口没有映射。我重新用 -p 8080:80 启动容器，再用 curl localhost:8080 测试，能够返回页面内容，浏览器也可以访问。这个问题让我知道了容器内部端口和宿主机端口不一样，但我没有继续配置防火墙、域名、HTTPS 或监控。

### OPS-02 工具会用但流程不足型

**第 1 轮**

我的目标职业是运维工程师。我学习过 Docker、Nginx、GitLab CI 和基础 Shell，能够写 Dockerfile 构建 Java 服务镜像，也跟着示例配置过流水线，把镜像推送后部署到测试服务器。我知道 Nginx 反向代理、环境变量和日志查看的基本方法，也会使用 docker compose 管理多个服务。当前不足是配置主要按已有模板修改，对制品版本、环境隔离、回滚策略、日志聚合、监控告警和安全策略理解不深。我希望系统根据真实工作内容判断，不要因为我列出了很多工具就直接认为我具备完整运维能力。

**第 2 轮**

一次测试环境发布中，流水线按拉取代码、Maven 打包、构建镜像、推送镜像仓库、SSH 登录服务器更新容器的顺序执行。新镜像启动失败后，我先停止继续发布，查看 docker logs，发现是环境变量没有配置完整。为了恢复服务，我重新运行上一个可用镜像标签，再补齐变量后重新执行发布。这个过程中我会记录镜像标签和报错信息，但回滚是手工完成的，没有自动健康检查，也没有把日志集中到统一平台。

### OPS-03 运行保障进阶型

**第 1 轮**

我的目标职业是运维工程师。我参与过校内服务平台的运行维护，负责 Docker Compose 部署、Nginx 反向代理、Prometheus 指标采集和 Grafana 看板配置。我为主机 CPU、内存、接口错误率配置过基础告警，能通过容器日志、应用日志和监控曲线定位常见问题，也参与过数据库连接数耗尽的排查。日常能完成服务发布、配置检查和故障记录，但没有真正管理 Kubernetes 生产集群，也没有形成基础设施即代码、容量规划和完整安全审计能力。请结合可验证工作内容判断我的岗位能力缺口。

**第 2 轮**

数据库连接数耗尽时，Grafana 显示连接数持续接近上限，同时接口 5xx 错误率上升。我先查看应用日志和连接池配置，发现一个批处理任务执行后没有及时释放连接；处理时暂停该任务、重启异常实例，并持续观察接口延迟。随后开发同学修复连接关闭逻辑，我协助调整连接池上限和告警阈值。恢复后连接数、错误率和平均响应时间都回到正常范围。这次我能说明排查过程，但对跨集群容灾和自动扩缩容还没有实践。

---

## 产品经理

### PM-01 产品基础起步型

**第 1 轮**

我的目标职业是产品经理。我学习过用户画像、需求分析、竞品分析、PRD 和原型设计的基础概念，会使用 Figma 绘制页面流程和简单交互原型。课程中我为校园失物招领项目做过功能列表、页面原型和需求说明，知道需要考虑发布、搜索和联系失主等流程。但我还没有正式访谈真实用户，没有参与研发评审、版本排期或上线后的数据分析，也不清楚怎样判断需求优先级。请根据我真正完成过的内容审查我的能力，不要把“会画原型”直接判断为已经具备完整产品经理能力。

**第 2 轮**

在失物招领项目里，我提出按物品类别和遗失地点筛选的需求。原因是同学试玩原型时，经常在较长列表里手动翻找，找到信息需要较多时间。我把筛选入口放在列表顶部，设计类别和地点两个条件，并让三位同学用原型完成指定物品查找任务；他们反馈比原来直接浏览列表更快。我根据这个结果保留了筛选功能，但没有正式上线，因此没有真实使用量、搜索成功率或用户留存数据，也没有和研发同学一起拆分技术实现。

### PM-02 有项目经历但证据不足型

**第 1 轮**

我的目标职业是产品经理。我在校园活动报名小程序中担任产品负责人，完成过需求收集、用户流程图、低保真和高保真原型、PRD 编写，并和前端、后端同学沟通过排期。项目包含活动浏览、报名、审核和消息通知功能，我也参与了测试验收。我的困难是需求会临时变化，我不知道怎样用事实和数据确定优先级，很多决定依靠讨论结果；上线后也没有形成正式复盘。请审查我目前的产品能力，区分已完成的项目证据、只会概念的部分和我需要进一步说明的部分。

**第 2 轮**

有一次活动方希望临时增加“多人代报名”。我先确认真实场景和截止时间，发现主要是社团负责人需要统一提交成员信息，而不是所有个人用户都需要该功能。为了不影响原有报名流程，我和研发讨论后决定只支持负责人批量导入，不改个人报名入口。我在 PRD 中补充了范围、风险和异常提示，上线后统计导入成功情况并收集负责人反馈，重复报名问题明显减少。不过当时没有设计完整埋点，也没有用固定的优先级模型记录决策。

### PM-03 数据驱动进阶型

**第 1 轮**

我的目标职业是产品经理。我参与过社区团购小程序的下单转化优化，负责用户访谈、竞品拆解、PRD、原型、埋点方案、研发跟进和版本复盘。通过漏斗数据发现购物车到提交订单的转化偏低后，我和设计、研发讨论配送费提示和凑单推荐方案，并推动上线。我能使用基础 SQL 查看用户分层和漏斗数据，也会同步排期、风险和客服反馈。当前我希望补强的是更严格的实验设计、商业目标拆解和多产品线策略，而不是只停留在功能需求编写层面。

**第 2 轮**

这次优化中，我把购物车进入提交页比例、提交页到支付成功比例和客单价作为核心指标，并按新老用户、活动渠道做分组比较。为了减少外部影响，上线前后一周避开大促日期，同时检查价格和库存没有明显变化。结果显示支付成功比例提升 6.8%，客单价没有下降，客服中关于配送费的咨询也减少，因此我建议把方案推广到其他城市。我的不足是没有做严格 A/B 实验，样本量和显著性判断还需要继续学习。
