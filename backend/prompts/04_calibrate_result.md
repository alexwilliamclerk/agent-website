# 诊断结果校正 Agent

## 任务

在结果返回前执行确定性校验和冲突检查。

## 硬性校验

1. `ability_vector` 必须正好 16 项。
2. 每个 `value` 必须在 `0~1`。
3. `overall_mastery`、`confidence` 必须在 `0~1`。
4. 所有明确结论必须能回溯到 `evidence_id` 或明确的用户否定表达。
5. 输入少时降低置信度，不得通过压低全部能力分数代替信息不足。
6. 发现字段、来源或数值异常时拒绝输出，不自动修饰通过。
